## Codebase Patterns
- All 74 roles are under roles/; use `find roles -path "*/meta/main.yml"` to enumerate meta files
- ansible-playbook play.yml --syntax-check is the quality gate (no inventory needed, warnings about empty inventory are normal)
- Existing meta/main.yml files may have galaxy_info blocks; append allow_duplicates: false rather than rewriting
- The 16 roles that already had meta/main.yml before US-001: cursor-theme, oneui4_icons, laptop, microcode, bluetooth, nmtrust, mirrorlist, goesimage, homebrew, gnome, uv_python_packages, microtex, gdm, ssh, screensaver, base

---

## 2026-03-23 - US-001
- Created meta/main.yml with `dependencies: []` and `allow_duplicates: false` for 58 roles that lacked one
- Added `allow_duplicates: false` to 16 roles that already had meta/main.yml (some had galaxy_info blocks, appended the key)
- All 74 roles now have meta/main.yml with both required keys
- ansible-playbook play.yml --syntax-check passes
- Files changed: 74 roles/*/meta/main.yml (58 created, 16 modified)
- **Learnings for future iterations:**
  - The for-loop from PRD notes only creates files that don't exist; existing files needed separate grep+append treatment
  - grep -L can verify which files are missing a key across all 74 files at once
  - No inventory file is needed for --syntax-check; the "no inventory" warnings are harmless
---

## 2026-03-23 - US-002
- Created scripts/validate_deps.py (executable, ~80 lines) with PyYAML-based dependency graph validation
- Added `make validate-deps` target to Makefile with `## ...` comment (appears in make help)
- Validator detects: MISSING (dep refs nonexistent role) and CYCLE (DFS cycle detection)
- Files changed: scripts/validate_deps.py (created), Makefile (added validate-deps target)
- **Learnings for future iterations:**
  - Ansible meta/main.yml deps can be strings OR dicts with `role:` key; handle both forms
  - The DFS cycle detection must skip nodes already reported as MISSING (no entry in graph)
  - `make help` is auto-generated from `## comment` on targets — just add the comment
  - PyYAML is available on the system (no extra install needed)
---

## 2026-03-23 - US-003
- Added `validate-deps` as prerequisite to `configure` target in Makefile (one-line change)
- Files changed: Makefile
- **Learnings for future iterations:**
  - configure target had `req-playbook` as only prereq; added `validate-deps` after it
  - Verified: broken dep graph aborts validate-deps with exit 1 before ansible-playbook runs
  - Roles with dict-style deps `{ role: name }` already worked; only needed to check string-style deps in test
---

## 2026-03-23 - US-004
- Set `dependencies: [{role: x}]` in roles/i3/meta/main.yml and roles/lightdm/meta/main.yml
- Files changed: roles/i3/meta/main.yml, roles/lightdm/meta/main.yml
- Verified: `ansible-playbook play.yml --tags i3 --list-tasks` shows x tasks before i3 tasks
- **Learnings for future iterations:**
  - Ansible meta dependencies cause the dep role's tasks to run even when only the dependent tag is specified
  - `--list-tasks` with a tag shows tasks from dep roles tagged under the dependent tag automatically
---

## 2026-03-23 - US-005
- Set `dependencies: [{role: gpu_detect}]` in roles/gpu_drivers/meta/main.yml
- Files changed: roles/gpu_drivers/meta/main.yml
- Verified: `ansible-playbook play.yml --tags gpu_drivers --list-tasks` shows gpu_detect tasks before gpu_drivers tasks
---

## 2026-03-23 - US-006
- Set hyprland deps to [wayland, qt_gtk_toolkit, widgets], qt_gtk_toolkit dep to [wayland], widgets dep to [qt_gtk_toolkit]
- uv_python_packages, microtex, oneui4_icons, screencapture reviewed - no wayland tasks found, no deps added
- Files changed: roles/hyprland/meta/main.yml, roles/qt_gtk_toolkit/meta/main.yml, roles/widgets/meta/main.yml
- **Learnings for future iterations:**
  - Diamond dependencies (hyprland→wayland, hyprland→qt_gtk_toolkit→wayland) cause `--list-tasks` to display wayland tasks multiple times
  - `allow_duplicates: false` handles actual runtime deduplication; `--list-tasks` display duplicates are cosmetic/known Ansible behavior
  - The correct order (wayland→qt_gtk_toolkit→widgets→hyprland) IS visible in --list-tasks despite apparent duplicates
---

## 2026-03-23 - US-007
- Reviewed base system roles: base, microcode, grub, systemd, system, filesystem, sysmon, cron, gnupg, regdomain, mirrorlist
- base→systemd, microcode→grub, mirrorlist→systemd all confirmed correct (pre-existing); added explanatory comments
- systemd role has handlers only (no tasks); other 7 roles (grub, system, filesystem, sysmon, cron, gnupg, regdomain) confirmed standalone
- Files changed: roles/base/meta/main.yml, roles/microcode/meta/main.yml, roles/mirrorlist/meta/main.yml
- **Learnings for future iterations:**
  - The `systemd` role is handlers-only; roles that use systemd handlers should depend on it
  - Pre-existing dict-style deps `{ role: name }` were migrated to `role: name  # comment` format
---
