---
- name: Install rcm on Arch
  become: true
  kewlfft.aur.aur:
    name: rcm
    use: auto
    state: present
  become_user: aur_builder
  when: ansible_facts["os_family"] == "Archlinux"
  tags:
    - aur

- name: Install rcm on Ubuntu or Debian
  ansible.builtin.package:
    name: rcm
    state: present
  when: ansible_facts["os_family"] != "Archlinux"
  become: true

- name: Clone user dotfiles
  become: true
  ansible.builtin.git:  # noqa: latest[git]
    repo: "{{ dotfiles.repo_url }}"
    dest: "/home/{{ dotfiles.path }}"
    accept_hostkey: true
    version: HEAD
    update: false
    recursive: true
  when: dotfiles is defined
  become_user: "{{ user.name }}"

- name: Symlink rcm rc file to user directory
  ansible.builtin.file:
    src: "/home/{{ dotfiles.path }}/rcrc"
    dest: "/home/{{ user.name }}/.rcrc"
    state: link

- name: Install user dotfiles
  become: true
  ansible.builtin.command: rcup -d /home/{{ dotfiles.path }} {{ dotfiles.rcup_flags }}
  changed_when: true
  when: dotfiles is defined
  become_user: "{{ user.name }}"

- name: Install linux gui tagged dotfiles
  become: true
  ansible.builtin.command: rcup -d /home/{{ dotfiles.path }} -tag linux-gui
  changed_when: true
  become_user: "{{ user.name }}"
  when: dotfiles.linuxgui | bool

- name: Create global gitconfig
  ansible.builtin.template:
    src: templates/gitconfig.j2
    dest: "/home/{{ user.name }}/.gitconfig"
    force: true
    mode: '0644'
